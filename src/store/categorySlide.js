import { createSlice } from "@reduxjs/toolkit";
const categorySlide = createSlice({
  name: "category",
  initialState: {},
  reducers: {
    showModal(state) {},
    hideModal(state) {},
  },
});

export default categorySlide;
